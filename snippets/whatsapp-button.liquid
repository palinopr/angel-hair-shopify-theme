{% comment %}
  Angel Hair Theme - WhatsApp Floating Button
  Enhanced with tooltip and smooth animations
{% endcomment %}

<style>
  .whatsapp-button {
    position: fixed;
    bottom: 24px;
    right: 24px;
    z-index: 90;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, #25D366 0%, #128C7E 100%);
    color: white;
    border-radius: 50%;
    box-shadow: 0 4px 20px rgba(37, 211, 102, 0.4);
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    animation: whatsapp-entrance 0.6s ease-out 1s both;
  }

  @keyframes whatsapp-entrance {
    from {
      opacity: 0;
      transform: scale(0) rotate(-180deg);
    }
    to {
      opacity: 1;
      transform: scale(1) rotate(0);
    }
  }

  .whatsapp-button:hover {
    transform: scale(1.1);
    box-shadow: 0 6px 30px rgba(37, 211, 102, 0.5);
    color: white;
  }

  .whatsapp-button:active {
    transform: scale(0.95);
  }

  .whatsapp-button__icon {
    width: 32px;
    height: 32px;
    transition: transform 0.3s ease;
  }

  .whatsapp-button:hover .whatsapp-button__icon {
    transform: rotate(15deg);
  }

  /* Tooltip */
  .whatsapp-button__tooltip {
    position: absolute;
    right: 70px;
    top: 50%;
    transform: translateY(-50%);
    background: #1a1a1a;
    color: white;
    padding: 10px 16px;
    border-radius: 8px;
    font-size: 0.875rem;
    font-weight: 500;
    white-space: nowrap;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
    pointer-events: none;
  }

  .whatsapp-button__tooltip::after {
    content: '';
    position: absolute;
    left: 100%;
    top: 50%;
    transform: translateY(-50%);
    border: 6px solid transparent;
    border-left-color: #1a1a1a;
  }

  .whatsapp-button:hover .whatsapp-button__tooltip {
    opacity: 1;
    visibility: visible;
    right: 75px;
  }

  /* Pulse ring animation */
  .whatsapp-button__pulse {
    position: absolute;
    inset: 0;
    border-radius: 50%;
    background: #25D366;
    z-index: -1;
    animation: whatsapp-pulse 2s ease-out infinite;
  }

  @keyframes whatsapp-pulse {
    0% {
      transform: scale(1);
      opacity: 0.5;
    }
    100% {
      transform: scale(1.8);
      opacity: 0;
    }
  }

  /* Badge notification */
  .whatsapp-button__badge {
    position: absolute;
    top: -4px;
    right: -4px;
    width: 20px;
    height: 20px;
    background: #ff3b30;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 11px;
    font-weight: 700;
    color: white;
    animation: badge-bounce 2s ease infinite;
  }

  @keyframes badge-bounce {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.1); }
  }

  /* Mobile adjustments */
  @media (max-width: 768px) {
    .whatsapp-button {
      bottom: 16px;
      right: 16px;
      width: 56px;
      height: 56px;
    }

    .whatsapp-button__tooltip {
      display: none;
    }
  }

  /* Hide when scrolling up on mobile */
  @media (max-width: 768px) {
    .whatsapp-button.is-hidden {
      transform: translateY(100px);
      opacity: 0;
    }
  }
</style>

{%- if settings.whatsapp_enabled and settings.whatsapp_number != blank -%}
  <a 
    href="https://wa.me/{{ settings.whatsapp_number }}?text={{ settings.whatsapp_message | url_encode }}" 
    class="whatsapp-button"
    target="_blank"
    rel="noopener noreferrer"
    aria-label="Contact us on WhatsApp"
    data-whatsapp
  >
    <span class="whatsapp-button__pulse"></span>
    <svg class="whatsapp-button__icon" viewBox="0 0 24 24" fill="currentColor">
      <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
    </svg>
    <span class="whatsapp-button__tooltip">{{ settings.whatsapp_tooltip | default: 'Chat with us!' }}</span>
    {%- if settings.whatsapp_show_badge -%}
      <span class="whatsapp-button__badge">1</span>
    {%- endif -%}
  </a>
{%- endif -%}

<script>
  // Hide WhatsApp button while scrolling on mobile
  (function() {
    if (window.innerWidth > 768) return;
    
    const whatsappBtn = document.querySelector('.whatsapp-button');
    if (!whatsappBtn) return;
    
    let lastScrollY = window.scrollY;
    let ticking = false;
    
    function updateButton() {
      const currentScrollY = window.scrollY;
      
      if (currentScrollY > lastScrollY && currentScrollY > 100) {
        whatsappBtn.classList.add('is-hidden');
      } else {
        whatsappBtn.classList.remove('is-hidden');
      }
      
      lastScrollY = currentScrollY;
      ticking = false;
    }
    
    window.addEventListener('scroll', function() {
      if (!ticking) {
        requestAnimationFrame(updateButton);
        ticking = true;
      }
    }, { passive: true });
  })();
</script>
